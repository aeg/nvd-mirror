[project]
name = "nvd-mirror"
version = "0.1.0"
description = "Mirror CVE records from the NVD CVE API 2.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "tomli; python_version < '3.11'",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff>=0.15.12",
]
